#import "../symbols.typ": *

== Свойства КС-языков. Разрешающие свойства. Алгоритм Кока-Янгера-Касами. Свойства замкнутости

== Разрешающие свойства КС-языков

Обычно, когда мы говорим о КС-языках, мы имеем в виду их представление в виде КС-грамматики или МП-автомата, принимающих строки по переходу в конечное состояние или опустошением стека. Существуют алгоритмы, решающие:
1. Принадлежит ли строка w КС-языку L.
2. Является ли КС-язык пустым.
3. Является ли КС-язык бесконечным.

/ 1. Проблема принадлежности ($w in L$?):
*Разрешима.* Проверяется алгоритмом Кока-Янгера-Касами (CYK).

Необходимо узнать, принадлежит ли строка $w$ языку $L(G)$. Предположим $G$ находится в НФХ. Или преобразуем данную грамматику в НФХ. $w = epsilon$ является специальным случаем, разрешаемым посредством проверки того, является ли начальный символ пустым.

*Алгоритм CYK (Кока-Янгера-Касами)* является хорошим примером динамического программирования и работает время порядка $O(n^3)$, где $n = |w|$.

- Пусть $w = a_1 ... a_n$.
- Строим треугольный массив множеств переменных со стороной $n$. В матрице на месте $(i,j)$: $X_{i,j} =$ {переменные $A$ | $A =>^* a_i ... a_j$}.
- Индукция по длине выводимой строки $j-i+1$.
- Начинаем с $X_{i,i}$, который выводит $a_i$.
- Затем находим $X_{i,i+1}$, который выводит $a_i a_{i+1}$. И т.д.
- Наконец, находим $X_{1,n}$ и спрашиваем, есть ли $S$ в $X_{1,n}$.

- *Базис:* $X_{i,i} = {A | A -> a_i$ есть продукция$}$.
- *Индукция:* $X_{i,j} = {A |$ существует продукция $A -> B C$ и целое $k$, с $i <= k < j$, такое, что $B$ есть в $X_{i,k}$ и $C$ есть в $X_{k+1,j}$.

/ Пример CYK:
Грамматика: $S -> A B$, $A -> B C | a$, $B -> A C | b$, $C -> a | b$.
Строка $w = a b a b a$.
Строим таблицу $X_{i,j}$:
- $X_{1,1}={A,C}$, $X_{2,2}={B,C}$, $X_{3,3}={A,C}$, $X_{4,4}={B,C}$, $X_{5,5}={A,C}$
- $X_{1,2}={B,S}$ (из $A -> B C$, $B in X_{1,1}$ и $C in X_{2,2}$; $S -> A B$, $A in X_{1,1}$ и $B in X_{2,2}$)
- $X_{2,3}={A}$ (из $B -> A C$, $A in X_{2,2}$, $C in X_{3,3}$)
- $X_{3,4}={B,S}$, $X_{4,5}={A}$
- $X_{1,3}={A}$ (из $A -> B C$, $B in X_{1,2}$ и $C in X_{3,3}$)
- $X_{2,4}={B,S}$, $X_{3,5}={A}$
- $X_{1,4}={B,S}$ (из $S -> A B$, $A in X_{1,2}$ и $B in X_{3,4}$)
- $X_{2,5}={A}$
- $X_{1,5}={A}$ (из $A -> B C$, $B in X_{1,2}$ и $C in X_{3,5}$)
Так как $S not in X_{1,5}$, строка $w$ не принадлежит языку.

/ 2. Проблема пустоты ($L(G) = emptyset$?):
*Разрешима.* Мы уже делали это. Мы научились устранять бесполезные переменные. Если стартовый символ есть среди них, то КС-язык пуст, в противном случае — нет.

/ 3. Проблема бесконечности ($|L| = infinity$?):
*Разрешима.* Идея по сути та же, что и для регулярных языков. Использовать константу $n$ леммы о накачке. Если существует строка языка длины между $n$ и $2n-1$, то язык бесконечный; в противном случае — нет.

== Неразрешимые свойства КС-языков

Многие вопросы, разрешимые для регулярных языков, не могут быть разрешимы для КС-языков.

Следующие проблемы алгоритмически неразрешимы:
- Проблема эквивалентности: $L(G_1) = L(G_2)$?
- Проблема связности: $L(G_1) inter L(G_2) = emptyset$?
- Проблема регулярности: является ли КС-язык регулярным?
- Проблема неоднозначности: является ли КС-грамматика неоднозначной?
- Проблема полноты: $L(G) = Sigma^*$?

Нужна теория машин Тьюринга и неразрешимых задач, чтобы доказать отсутствие соответствующих алгоритмов.

== Свойства замкнутости КС-языков

КС-языки замкнуты относительно операций объединения, конкатенации и замыкания Клини (т.е. относительно регулярных операций). Также, КС-языки замкнуты относительно обратимости строк, гомоморфизма и обратного гомоморфизма. Но не по пересечению или разности.

Замыкание КС-языков по объединению.
Пусть $L$ и $M$ — КС-языки с грамматиками $G$ и $H$, соответственно. Предположим $G$ и $H$ не имеют общих переменных. Имена переменных не влияют на язык. Пусть $S_1$ и $S_2$ — начальные символы $G$ и $H$.

Образуем новую грамматику для $L union M$ путём комбинации всех символов и продукций $G$ и $H$. Затем, добавим новый начальный символ $S$. Добавим продукции $S -> S_1 | S_2$.

В построенной новой грамматике, все выводы начинаются с $S$. На первом шаге происходит замена $S$ на $S_1$ или $S_2$. В первом случае, результатом должна стать строка в $L(G) = L$, а во втором — строка в $L(H) = M$.

Замыкание КС-языков по конкатенации.
Пусть $L$ и $M$ — КС-языки с грамматиками $G$ и $H$, соответственно. Предположим $G$ и $H$ не имеют общих переменных. Пусть $S_1$ и $S_2$ — начальные символы $G$ и $H$.

Образуем новую грамматику для $L M$, начав с того, что включим в неё все символы и продукции $G$ и $H$. Добавим новый начальный символ $S$. Добавим продукцию $S -> S_1 S_2$. Каждый вывод из $S$ даст в результате строку в $L$ с последующей за ней строкой из $M$.

Замыкание КС-языков по операции Клини.
Пусть $L$ имеет грамматику $G$, с начальным символом $S_1$. Образуем новую грамматику для $L^*$ путём введения в $G$ нового начального символа $S$ и продукций $S -> S_1 S | epsilon$. Правый вывод из $S$ генерирует последовательность из нуль или более переменных $S_1$, каждая из которых генерирует некоторую строку из $L$.

Замыкание КС-языков по обратимости.
Если $L$ — КС-язык с грамматикой $G$, образуем грамматику для $L^R$ путём обращения тела каждой продукции.

Пример: Пусть $G$ имеет $S -> 0 S 1 | 01$. Обращение языка $L(G)$ имеет грамматику $S -> 1 S 0 | 10$.

Замыкание КС-языков по гомоморфизму.
Пусть $L$ — КС-язык с грамматикой $G$. Пусть $h$ — гомоморфизм на терминальных символах $G$. Построим грамматику для $h(L)$, заменяя каждый терминальный символ $a$ на $h(a)$.

Пример: $G$ имеет продукции $S -> 0 S 1 | 01$. $h$ определяется как $h(0) = "ab"$, $h(1) = epsilon$. $h(L(G))$ имеет грамматику с правилами $S -> a b S | a b$.

Замыкание КС-языков по обратному гомоморфизму.
Здесь нам не помогут грамматики, но вполне подойдет конструкция МПА. Пусть $L = L(P)$ для некоторого МПА $P$. Построим МПА $P'$, принимающий $h^{-1}(L)$.

$P'$ моделирует $P$, но сохраняет, в качестве одной из частей двухкомпонентного состояния, буфер, который хранит результат применения $h$ к одному входному символу.

*Архитектура P':* Буфер + Состояние P. Читаем сначала оставшийся символ в буфере, как если бы это было входом для P.

*Формальное построение P':*
- Состояниями являются пары $[q, w]$, где: $q$ есть состояние $P$, $w$ есть суффикс $h(a)$ для некоторого символа $a$. Таким образом, только конечное число возможных значений для $w$.
- Стековые символы $P'$ — те же, что и у $P$.
- Начальным состоянием $P'$ является $[q_0, epsilon]$.
- Входные символы $P'$ — символы, к которым применяется $h$.
- Финальные состояния $P'$ — те состояния $[q, epsilon]$, для которых $q$ является финальным состоянием $P$.

*Переходы P':*
1. $delta'([q, epsilon], a, X) = {([q, h(a)], X)}$ для любого символа $a$ автомата $P'$ и любого стекового символа $X$. Когда буфер пуст, $P'$ может перезагрузить его.
2. $delta'([q, b w], epsilon, X)$ содержит $([p, w], alpha)$, если $delta(q, b, X)$ содержит $(p, alpha)$, где $b$ есть либо входной символ $P$, либо $epsilon$. Моделирование $P$ из буфера.

*Доказательство корректности P':* Нам нужно показать, что $L(P') = h^{-1}(L(P))$. Ключевой аргумент: $P'$ делает переход $([q_0, epsilon], w, Z_0) =>^* ([q, x], epsilon, alpha)$ тогда и только тогда, когда $P$ делает переход $(q_0, y, Z_0) =>^* (q, epsilon, alpha)$, $h(w) = y x$, и $x$ есть суффикс последнего символа $w$. Доказательство в обоих направлениях проводится индукцией по числу произведенных шагов.

Отсутствие замыкания по пересечению.
В отличие от регулярных языков, класс КС-языков не замкнут относительно пересечения. Мы знаем, что $L_1 = {0^n 1^n 2^n | n > 1}$ не есть КС-язык (использовали лемму о накачке). Однако, $L_2 = {0^n 1^n 2^i | n > 1, i > 1}$ КС-языком является. КСГ: $S -> A B$, $A -> 0 A 1 | 01$, $B -> 2 B | 2$. Это так и для $L_3 = {0^i 1^n 2^n | n > 1, i > 1}$. Но $L_1 = L_2 inter L_3$!

Отсутствие замыкания по разности.
Мы можем доказать нечто более общее: любой класс языков, замкнутый относительно разности, замкнут по пересечению. Доказательство: $L inter M = L - (L - M)$. Таким образом, если бы КС-языки были замкнуты по разности, то они были бы замкнуты и по пересечению, но это не так.

Пересечение КС-языка с регулярным языком.
Пересечение двух КС-языков не обязательно будет КС-языком. Но пересечение КС-языка с регулярным языком всегда будет КС-языком. Доказательство включает запуск работы КДА в параллели с МПА, и отмечая, что их комбинация является МПА. МПА принимает по переходу в заключительное состояние.

*Формальное построение.*
Пусть КДА $A$ имеет функцию переходов $delta_A$. Пусть МПА $P$ имеет функцию переходов $delta_P$. Состояниями комбинированного МПА являются $[q,p]$, где $q$ — состояние $A$ и $p$ — состояние $P$.

$delta([q,p], a, X)$ содержит $([delta_A(q,a), r], alpha)$ если $delta_P(p, a, X)$ содержит $(r, alpha)$. Заметим, что в качестве $a$ может быть $epsilon$, в этом случае $delta_A(q,a) = q$.

Финальными состояниями скомбинированного МП-автомата являются те $[q,p]$, в которых $q$ есть финальное состояние $A$ и $p$ есть финальное состояние $P$. Начальным состоянием является пара $[q_0, p_0]$, состоящая из начальных состояний каждого автомата.

Простая индукция: $([q_0, p_0], w, Z_0) =>^* ([q, p], epsilon, alpha)$ тогда и только тогда, когда $delta_A(q_0, w) = q$ и в $P$: $(p_0, w, Z_0) =>^* (p, epsilon, alpha)$.